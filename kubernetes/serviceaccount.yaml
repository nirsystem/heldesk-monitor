apiVersion: v1
kind: ServiceAccount
metadata:
  name: helpdesk-sa
  namespace: helpdesk
  annotations:
    # החלף ב-ARN של ה-IAM Role מ-Terraform outputs:
    # terraform output -raw helpdesk_irsa_role_arn
    eks.amazonaws.com/role-arn: "arn:aws:iam::YOUR_ACCOUNT_ID:role/helpdesk-irsa-role"
