apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: helpdesk-data-pvc
  namespace: helpdesk
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: efs-sc
